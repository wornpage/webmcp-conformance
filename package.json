{
  "name": "webmcp-conformance-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:portable": "npm run test:portable --workspace webmcp-conformance && npm test --workspace @webmcp-conformance/webmcp-discovery-host && npm test --workspace @webmcp-conformance/wornpage-consumer-check",
    "check": "npm run check --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
